Input two 4-bit numbers and select the mode. Open the clock, the result we be shown in 1 tick

the buffer zone in the circuit is to makesure the register will not read before the actual results come out.